# @karyl-chan packages (the plugin SDK) are published to GitHub Packages.
# You need a GitHub personal access token with read:packages scope; put it
# in your user ~/.npmrc as:
#   //npm.pkg.github.com/:_authToken=YOUR_TOKEN
@karyl-chan:registry=https://npm.pkg.github.com
